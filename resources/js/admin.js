// Abrir modal para criar proposta de INSS no menu de propostas de INSS:
function createProposal() {
    if (document.getElementById('createProposal').style.display == 'block') {
        document.getElementById('createProposal').style.display == 'none';
    } else {
        document.getElementById('createProposal').style.display == 'block'
    }
}